# 美团一面

## 开场
* 自我介绍
* 你的优点和缺点是什么

## Java
### java线程的通信方式
* 使用同一个共享变量控制
  * synchronized、wait、notify
  *  lock、condition
  *  volatile
  *  AtomicInteger
*  PipedInputStream、PipedOutputStream
*  BlockingQueue

### 什么事线程安全，怎么保持线程安全
* **定义** ：多个线程访问某一个类或对象的时候，对象对应的公共数据去始终都能表现正确，不会出现数据污染的情况
* 可能涉及线程安全的操作
  * 访问共享变量或资源
  * 依赖时序的操作
  * 不同数据之间存在绑定关系
  * 对方没有声明自己是线程安全的
#### 怎样保持线程安全
* 数据不可变
* 加锁

### synchronized修饰类方法和静态方法的区别
* 修饰普通方法：作用于当前对象实例，进入同步代码前要获得当前对象实例的锁
* 修饰静态方法：作用于当前类，进入同步代码前要获得当前class对象的锁
* 修饰代码块：指定加锁对象，给指定对象加锁，进入同步代码块前要获得给定对象的锁


### 堆排序
> 分为最大堆和最小堆，每一层必须填满，可用于优先队列
* 最大堆： 每个子节点小于父节点
* 最小堆： 每个子节点大于父节点
* 操作时间复杂度：O(ologn)


* 类和对象有什么区别
* 重载和重写有什么区别
### 怎样让一个方法不被子类重写
* final、static、private(加注解才会提示不行)


### 匿名内部类可以访问外部类的哪些变量
* final
### 你遇到过的最深刻的Exceptipn
#### 数组越界
> 使用迭代器


## Android
* Activity的生命周期
* 各个生命周期函数在什么时候被调用
* 两个Activity切换时，生命周期的方法怎样回调
* Activity怎样传递数据
* Fragment及常见的生命周期，调用时机
* 有什么方法让文字大小不受系统设置的字体大小影响
* Handler可以操作ui吗，还有什么方法可以实现改变ui操作
* ANR是什么，什么情况下会出现 
* MVC、MVP
* MVC中数据是怎样流转的，Activity在MVC中充当什么作用
* Java Swing和Android的区别，谈谈你的理解
* 

## 计网
* 七层网络模型和四层网络模型
* HTTP，TCP和UDP的区别、关系，工作在哪一层
* TCP是怎样做到可靠的
* http常用的请求方式
* get和post的区别


## 操作系统
* 进程和线程的区别
* 进程和程序的区别
* 进程的ipc机制


## 算法
* 对之前没完成的笔试题的思路
* 数量级大用dp需要考虑什么
* 手撕：接雨水

## 其他
* 你怎么快速掌握一门新的语言

## 反问环节